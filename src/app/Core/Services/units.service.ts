import { HttpClient } from '@angular/common/http';import { Injectable,inject } from '@angular/core';import { environment } from '../../../environments/environment';import { PaginatedResult,Unit } from '../Models/api.models';
@Injectable({providedIn:'root'})export class UnitsService{private http=inject(HttpClient);getAll(projectId:number,pageSize=50){return this.http.get<PaginatedResult<Unit>>(`${environment.apiUrl}/Projects/${projectId}/units`,{params:{PageSize:pageSize}});}getById(projectId:number,unitId:number){return this.http.get<Unit>(`${environment.apiUrl}/Projects/${projectId}/units/${unitId}`);}}
