import { inject } from '@angular/core';import { CanActivateFn,Router } from '@angular/router';import { TranslationService } from '../I18n/translation.service';import { AuthService } from '../Services/auth.service';import { NotificationService } from '../Services/notification.service';
export const authGuard:CanActivateFn=(_,state)=>{const auth=inject(AuthService);if(auth.isAuthenticated())return true;inject(NotificationService).show(inject(TranslationService).translate('loginRequired'),'info');return inject(Router).createUrlTree(['/auth/login'],{queryParams:{returnUrl:state.url}})};export const guestGuard:CanActivateFn=()=>!inject(AuthService).isAuthenticated()||inject(Router).createUrlTree(['/profile']);
