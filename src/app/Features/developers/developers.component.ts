import{ChangeDetectionStrategy,Component,inject,signal}from'@angular/core';import{Developer}from'../../Core/Models/api.models';import{DevelopersService}from'../../Core/Services/developers.service';import{SeoService}from'../../Core/Services/seo.service';import{TranslatePipe}from'../../Shared/Pipes/translate.pipe';
@Component({selector:'app-developers',imports:[TranslatePipe],template:`<section class="section"><div class="container"><p class="eyebrow">{{'trustPartners'|t}}</p><div class="section-heading"><div><h1>{{'realEstateDevelopers'|t}}</h1><p class="muted">{{'developersLead'|t}}</p></div></div><div class="dev-grid">@for(d of items();track d.id){<article><div class="avatar">{{d.name.charAt(0)}}</div><h2>{{d.name}}</h2><p class="muted">{{d.description||('developerFallback'|t)}}</p><div><strong>{{d.projectsCount}}</strong> {{'projects'|t}}</div>@if(d.websiteUrl){<a class="btn btn-ghost btn-sm" [href]="d.websiteUrl" target="_blank" rel="noopener">{{'visitWebsite'|t}}</a>}</article>}</div></div></section>`,styles:[`.dev-grid{display:grid;grid-template-columns:repeat(3,1fr);gap:1.2rem}.dev-grid article{padding:1.5rem;border:1px solid var(--color-border);border-radius:var(--radius-lg);background:var(--card-bg)}.avatar{display:grid;width:64px;height:64px;place-items:center;border-radius:50%;background:#30230e;color:var(--color-primary);font-size:1.5rem;font-weight:900}.dev-grid h2{margin:.8rem 0 .2rem}.dev-grid strong{color:var(--color-primary);font-size:1.4rem}.dev-grid .btn{margin-top:1rem}@media(max-width:820px){.dev-grid{grid-template-columns:1fr}}`],changeDetection:ChangeDetectionStrategy.OnPush})export class DevelopersComponent{readonly items=signal<Developer[]>([]);constructor(){inject(SeoService).update('المطورون العقاريون','تعرف على المطورين العقاريين في كيميت.','/developers');inject(DevelopersService).getAll().subscribe({next:r=>this.items.set(r.data),error:()=>{}})}}
