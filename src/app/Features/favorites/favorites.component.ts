import{ChangeDetectionStrategy,Component,inject,signal}from'@angular/core';import{Favorite}from'../../Core/Models/api.models';import{FavoritesService}from'../../Core/Services/favorites.service';import{SeoService}from'../../Core/Services/seo.service';import{ListingCardComponent}from'../../Shared/Components/listing-card/listing-card.component';import{TranslatePipe}from'../../Shared/Pipes/translate.pipe';
@Component({selector:'app-favorites',imports:[ListingCardComponent,TranslatePipe],template:`<section class="section"><div class="container"><p class="eyebrow">{{'yourChoices'|t}}</p><div class="section-heading"><div><h1>{{'favoriteProperties'|t}}</h1><p class="muted">{{'favoritesLead'|t}}</p></div></div>@if(items().length){<div class="grid-cards">@for(item of items();track item.favoriteId){<app-listing-card [item]="item" kind="property"/>}</div>}@else{<div class="empty-state">{{'noFavorites'|t}}</div>}</div></section>`,changeDetection:ChangeDetectionStrategy.OnPush})export class FavoritesComponent{readonly items=signal<Favorite[]>([]);constructor(){inject(SeoService).update('المفضلة','عقاراتك المفضلة على كيميت.','/favorites');inject(FavoritesService).getAll().subscribe({next:v=>this.items.set(v),error:()=>{}})}}
